# TODO
- [x] Implement edit + delete actions in `resources/views/admin/product.blade.php` using the existing add-product modal as a base.
- [x] Add backend endpoints in `routes/web.php` + controller methods in `app/Http/Controllers/ProductController.php` for update & soft delete.
- [x] Add Edit modal + JS to prefill form and keep old image when not uploading a new one.
- [x] Smoke test: update without image, update with image, delete product.


